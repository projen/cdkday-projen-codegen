// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as dynamodb from 'aws-cdk-lib/aws-dynamodb';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';
export class ProjectTable extends dynamodb.Table {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      partitionKey: { name: 'pid', type: dynamodb.AttributeType.STRING },
    });
  }
  public bind(handler: lambda.Function) {
    handler.addEnvironment('PROJECT_TABLE_NAME', this.tableName);
    this.grantReadWriteData(handler);
  }
}